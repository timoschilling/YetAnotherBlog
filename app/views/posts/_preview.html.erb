<p>
  <a href="<%= post_path(post) %>"><h3><%= post.headline %></h3></a>
  by <%= render post.user %> at <%= format_post_date(post.created_at) %>
</p>
<p>
  <%= render partial: "comments/comment", locals: { comment: post.comments.first } %>
</p>
